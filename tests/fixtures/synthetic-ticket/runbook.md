# Local testing runbook (synthetic)

## Unit tests

```bash
make test
```

## Notes

This fixture uses `verify.profiles.unit_local` style testing only.
It intentionally does **not** describe a cluster bring-up so OSS users
can practice distill without cloud credentials.
